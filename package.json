{
  "name": "isitpeak",
  "displayName": "IsItPeak?",
  "description": "通用的峰谷价标识插件",
  "version": "0.0.0",
  "publisher": "debug967",
  "repository": "https://github.com/lidongxun967/IsItPeak",
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "IsItPeak",
      "properties": {
        "isitpeak.peakPeriods": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "start": {
                "type": "string",
                "description": "开始时间（24 小时制，如 08:00）"
              },
              "end": {
                "type": "string",
                "description": "结束时间（24 小时制，如 11:00；若小于开始时间则视为跨天）"
              },
              "days": {
                "type": "array",
                "items": {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 6
                },
                "description": "该时段生效的星期（0=周日、1=周一、……、6=周六）；省略或为空则每天生效"
              }
            },
            "required": [
              "start",
              "end"
            ]
          },
          "default": [
            {
              "start": "09:00",
              "end": "12:00",
              "days": [1, 2, 3, 4, 5]
            },
            {
              "start": "14:00",
              "end": "18:00",
              "days": [1, 2, 3, 4, 5]
            }
          ],
          "description": "峰价时段列表，支持多个时段与跨天（如 22:00 至 06:00）"
        },
        "isitpeak.peakLabel": {
          "type": "string",
          "default": "峰价",
          "description": "处于峰价时段时状态栏显示的文字"
        },
        "isitpeak.valleyLabel": {
          "type": "string",
          "default": "谷价",
          "description": "非峰价时段时状态栏显示的文字"
        },
        "isitpeak.notifyOnSwitch": {
          "type": "boolean",
          "default": true,
          "description": "状态在峰价/谷价之间切换时是否弹出通知"
        },
        "isitpeak.showRemaining": {
          "type": "boolean",
          "default": true,
          "description": "是否在状态栏直接显示当前状态剩余时长（精确到分钟，如「峰价还剩2小时30分钟」）"
        },
        "isitpeak.peakYellowBackground": {
          "type": "boolean",
          "default": true,
          "description": "处于峰价时段时是否使用黄色背景高亮状态栏"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "compile": "pnpm run check-types && pnpm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "pnpm run check-types && pnpm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "@types/vscode": "^1.125.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "eslint": "^10.5.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.1"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
